spring.application.name=article-crud

# Configuración de MySQL. Dejamos esta configuración por defecto.
spring.datasource.url=jdbc:mysql://localhost:3306/article_crud?useSSL=false
spring.datasource.username=root
spring.datasource.password=root

# Actualiza el esquema de la base de datos automáticamente al iniciar la aplicación (no recomendado en producción)
spring.jpa.hibernate.ddl-auto=update

#Fin configuración por defecto

# Opcional: Mostrar las consultas en consola
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Configuración para testear la app.
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=create-drop
#Fin configuración para test

